type Course @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  desc: String
  createdBy: String
  createdAt: String
}

type User @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  email: String
}
 